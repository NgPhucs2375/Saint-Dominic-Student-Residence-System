// Bảng màu: Swiss Minimalist & Royal Earthy
export const C = {
  bg: "#F9F9F6",          // Trắng gạo / Alabaster (Không dùng trắng tinh gây mỏi mắt)
  sidebar: "#1A1A1A",     // Đen than chì / Deep Charcoal (Quyền lực, sâu thẳm)
  sidebarText: "#D4D4D0", // Xám khói nhạt (Đọc trên nền tối không bị gắt)
  
  card: "#FFFFFF",        // Trắng tinh khiết (Làm nổi bật các khối thẻ)
  cardHov: "#F2F2ED",     // Xám kem nhẹ (Hiệu ứng lướt chuột)
  
  border: "#E6E6E1",      // Xám xi măng nhạt (Ranh giới thanh mảnh)
  borderDark: "#2B2B2B",  // Viền cho khu vực Sidebar tối
  
  text: "#1D1D1B",        // Đen ánh nâu siêu nhẹ (Chữ chính)
  muted: "#8C8A86",       // Xám rêu nhạt (Chữ phụ)
  
  gold: "#B59868",        // Vàng kim trầm / Antique Gold (Tuyệt đối không neon)
  goldLight: "#DFD1B8",   // Vàng kem nhạt
  brown: "#6B5A4E",       // Nâu Mocha
  
  // Màu trạng thái & Biểu đồ (Đã chuyển sang hệ màu Pastel trầm)
  success: "#5A7D69",     // Xanh rêu trầm / Sage Green
  warn: "#C28C52",        // Cam đất / Terracotta
  danger: "#A65A54",      // Đỏ gạch / Rust
  blue: "#5C758A",        // Xanh lam trầm / Slate Blue
  purple: "#807182",      // Tím khói / Dusty Plum
};

// Bóng đổ tinh tế như ánh sáng tự nhiên
export const SHADOW = "0 8px 30px -4px rgba(0, 0, 0, 0.03), 0 4px 10px rgba(0, 0, 0, 0.01)";
export const SHADOW_HOV = "0 15px 35px -5px rgba(181, 152, 104, 0.15), 0 5px 15px rgba(0, 0, 0, 0.03)";